# Database Configuration
# IMPORTANT: Use different hostnames based on context:
#   - LOCAL DEVELOPMENT: localhost (direct connection to local PostgreSQL)
#   - DOCKER CONTAINERS: db (Docker service name for inter-container communication)
#   - PRODUCTION: your-rds-endpoint.aws.com (or managed database endpoint)
#
# Example values:
#   LOCAL:  postgresql+asyncpg://postgres:postgres@localhost:5432/footnote
#   DOCKER: postgresql+asyncpg://postgres:postgres@db:5432/footnote
#   PROD:   postgresql+asyncpg://user:password@your-db.rds.amazonaws.com:5432/footnote
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/footnote

# Google OAuth Configuration
# Get these from https://console.cloud.google.com/apis/credentials
# Create a new OAuth 2.0 Client ID (Web application)
GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret-here
GOOGLE_REDIRECT_URI=http://localhost:8000/api/auth/google/callback

# Fireworks AI - for embeddings and reranking
# Get API key from https://fireworks.ai/
FIREWORKS_API_KEY=your-fireworks-api-key-here

# Anthropic Claude - for generation
# Get API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Mistral - for PDF OCR
# Get API key from https://console.mistral.ai/
MISTRAL_API_KEY=your-mistral-api-key-here

# Security Configuration
# Generate a secure random string: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=generate-a-secure-random-string-here
SESSION_EXPIRE_HOURS=24

# Frontend Configuration
FRONTEND_URL=http://localhost:3000
CORS_ORIGINS=["http://localhost:3000"]
